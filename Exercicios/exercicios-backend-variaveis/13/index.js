let raio = 3;
let altura = 1;
let pi = "pi";

let areaTotal = 2 * raio * (raio + altura) + pi;

console.log(
  `Um cilindro com ${raio} de raio e ${altura} de altura possui uma área total de ${areaTotal}.`
);
